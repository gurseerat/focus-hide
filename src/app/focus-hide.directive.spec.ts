import { FocusHideDirective } from './focus-hide.directive';

describe('FocusHideDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusHideDirective();
    expect(directive).toBeTruthy();
  });
});
